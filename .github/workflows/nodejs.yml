name: Node CI

on:
  push:
    branches:
    - master

jobs:
  my-job:
    name: My Job
    runs-on: ubuntu-latest
    steps:
    - name: test action
      env:
        MY_VAR: Hi there! My name is
        FIRST_NAME: Mona
        MIDDLE_NAME: The
        LAST_NAME: Octocat
      run: |
        ls && pwd
